# Acceptance criteria

- [ ] Device Admin receiver and Device Owner checks are implemented.
- [ ] Development provisioning commands are documented.
- [ ] Policy operations are isolated behind a small interface.
- [ ] Provisioning failures produce recoverable, clear states.
- [ ] Tests cover policy-controller logic that can be tested off-device.

## Required validation commands

- `cd src; .\gradlew.bat test lint`
