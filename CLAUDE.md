# Claude Code entrypoint

Do not treat this file as a duplicate project specification.

1. Read `AGENTS.md`.
2. Read `PROJECT_STATE.yaml`.
3. Open the active stage's `stage.yaml` and assigned persona files only.
4. Follow the stage context and source scope exactly.
5. Do not perform broad repository discovery unless the active stage explicitly permits it.
